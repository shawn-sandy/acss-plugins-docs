---
title: /kit-list
description: Browse the full component catalogue or inspect a specific component — read-only, no side effects.
---

import { Aside } from "@astrojs/starlight/components";

Browse the component catalogue or inspect a specific component's props, CSS tokens, dependencies, and generation contract. This command is entirely read-only — it never writes files.

## Syntax

```text
/kit-list [component]
```

## Arguments

| Argument    | Required | Description                                                      |
| ----------- | -------- | ---------------------------------------------------------------- |
| `component` | No       | Component name to inspect. If omitted, shows the full catalogue. |

## Examples

List all available components:

```text
/kit-list
```

Inspect a specific component:

```text
/kit-list dialog
/kit-list button
/kit-list card
```

## How `/kit-list` works

As of v1.2.0, component inventory is sourced from per-component skills only — `catalog.md` has been removed. The command globs `skills/component-*/SKILL.md` and reads the `name:` and `description:` frontmatter from each. Lowercase the argument before lookup (e.g. `/kit-list Button` → looks for `button`).

The full workflow is documented in the [`kit-core skill`](/acss-plugins-docs/acss-kit/skills/kit-core).

## Output: full catalogue

Without a component name, `/kit-list` prints every available component organized by category. The 15 per-component skills are:

```
Available Components (acss-kit v1.2)

Leaf components (no dependencies):
  alert       — Accessible status/error/info/warning notification with ARIA live regions
  button      — Accessible button with primary/secondary/destructive/ghost variants
  card        — Content container with header/body/footer slots
  field       — Form field wrapper with label, hint, and error association
  icon        — SVG icon wrapper with decorative/semantic modes
  img         — Accessible image with required alt and aspect-ratio containment
  input       — Text/number/email input with aria-invalid and error state
  link        — Anchor with external/new-tab detection and current-page indication
  list        — Ordered/unordered list with role="list" reset
  nav         — Navigation landmark with aria-label and current-page marking
  popover     — Popover API popover with focus trap and light-dismiss
  table       — Data table with caption, scope headers, and sortable columns

Components with dependencies:
  checkbox    — Checkbox with indeterminate state (depends on: input)
  dialog      — Modal dialog with focus trap (depends on: button)
  icon-button — Icon-only button with required aria-label (depends on: button)

Inline-only (no dedicated skill):
  badge, tag, heading, text/paragraph, details, progress
  → Use /kit-create to generate these from a natural-language description.

Run /kit-add <component> to generate React components.
```

## Output: component inspect

With a component name, `/kit-list` shows the full specification including a dedicated **HTML output** line:

```
Component: Button
File: button.tsx + button.scss
Dependencies: none (simple component)
HTML output: Verified (/kit-add --target=html can generate this component)

Props:
  children   ReactNode         required  Button content
  variant    primary|secondary|ghost|danger  optional  default: 'primary'
  size       sm|md|lg          optional  default: 'md'
  disabled   boolean           optional  default: false
  onClick    MouseEventHandler  optional  Click handler
  className  string            optional  Additional CSS classes

CSS tokens:
  --btn-bg           Background color       var(--color-primary, #4f46e5)
  --btn-color        Text color             var(--color-text-inverse, #fff)
  --btn-border       Border color           transparent
  --btn-radius       Border radius          var(--radius-md, 0.375rem)
  --btn-padding-x    Horizontal padding     1rem
  --btn-padding-y    Vertical padding       0.5rem
  --btn-font-size    Font size              1rem
  --btn-font-weight  Font weight            600

Usage:
  <Button variant="primary" onClick={handler}>Save</Button>
  <Button variant="secondary">Cancel</Button>
  <Button disabled>Unavailable</Button>
```

For a component without HTML support the line reads: `HTML output: Not yet — React only (/kit-add --target=html will warn)`.

## Reading the generation contract

The generation contract tells you exactly what `/kit-add <component>` will create, including the resolved dependency tree. This lets you preview the output before running the write command.

<Aside type="tip">
Use `/kit-list <component>` as a planning step before `/kit-add`. You can see the full dependency chain and all CSS tokens without touching your project files.
</Aside>

## No side effects

`/kit-list` never creates, modifies, or deletes any file. It's safe to run at any time, in any project state.
