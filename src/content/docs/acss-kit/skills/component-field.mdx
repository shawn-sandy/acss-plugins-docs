---
title: component-field skill
description: Generate an accessible Field component — form field wrapper with label, hint, and error message association via aria-describedby — directly into your project.
---

import { Steps } from "@astrojs/starlight/components";
import ComponentPreview from "~/components/ComponentPreview.astro";

The `component-field` skill generates an fpkit-style accessible Field component directly into a developer's project. It is invoked by [`/kit-add field`](/acss-plugins-docs/acss-kit/commands/kit-add), [`/kit-create`](/acss-plugins-docs/acss-kit/commands/kit-create) (when asking for a field), or by calling the `component-field` skill by name.

|                  |                                                                                               |
| ---------------- | --------------------------------------------------------------------------------------------- |
| **Triggered by** | `/kit-add field`, `/kit-create` → "field", or direct skill invocation                         |
| **Output**       | `<targetDir>/field.tsx` + `<targetDir>/field.module.scss`                                     |
| **Dependencies** | None — leaf component                                                                         |
| **Hint**         | Describe whether you need hint text, an error message slot, and whether the field is required |

## Workflow

<Steps>

1. **Read** `reference.md` in the `component-field` skill directory for the canonical TSX/SCSS templates, Generation Contract, Props Interface, CSS Variables, Accessibility requirements, and Usage Examples.

2. **Init check** — run `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/detect_target.py <cwd>`. If the foundation (`ui.tsx`) is missing, run Step A of the [`kit-core` skill](/acss-plugins-docs/acss-kit/skills/kit-core) to initialize it before generating this component.

3. **Dependencies** — Field has no upstream dependencies. Skip dep resolution.

4. **Generate** — apply the Generation Contract from `reference.md` to produce:
   - `<targetDir>/field.tsx` from the `## TSX Template` section
   - `<targetDir>/field.module.scss` from the `## SCSS Template` section

5. **Verify** — run `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/verify_integration.py <projectRoot>` and print the summary to the developer.

</Steps>

## Invocation hints

```text
/kit-add field
```

```text
/kit-create — "create a field wrapper with hint text and an error message slot"
```

## CSS variable prefix

Field tokens follow the `--field-*` naming convention, e.g. `--field-label-color`, `--field-hint-color`, `--field-error-color`.

## Accessibility

Field associates its label, hint, and error message with the wrapped input via `aria-describedby`. Required fields use `aria-required="true"` on the inner control.

## Example output

<ComponentPreview
  code={`<div style="display:flex;flex-direction:column;gap:0.25rem;max-width:320px">
  <label for="email" style="font-weight:600;font-size:0.875rem">Email address <span aria-hidden="true" style="color:#ef4444">*</span></label>
  <input id="email" type="email" aria-required="true" aria-describedby="email-hint" style="padding:0.5rem;border:1px solid #d1d5db;border-radius:0.25rem" placeholder="you@example.com" />
  <span id="email-hint" style="font-size:0.75rem;color:#6b7280">We'll never share your email.</span>
</div>`}
  label="Field component preview"
/>

## Related

- [/kit-add](/acss-plugins-docs/acss-kit/commands/kit-add)
- [kit-core skill](/acss-plugins-docs/acss-kit/skills/kit-core)
- [Component Catalogue](/acss-plugins-docs/acss-kit/component-catalogue)
