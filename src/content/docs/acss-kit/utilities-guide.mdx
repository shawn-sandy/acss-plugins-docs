---
title: Utilities — Consumer Guide
description: How to use acss-kit's atomic CSS utility classes — adding the bundle, class usage, responsive variants, and the token bridge that wires utility color classes to your theme.
---

import { Aside, Steps } from "@astrojs/starlight/components";
import ColorSwatch from "~/components/ColorSwatch.astro";

Tailwind-style atomic CSS utility classes for fpkit/acss projects. A single prebuilt `utilities.css` bundle ships the full atomic suite — spacing, flexbox, grid, color, type, radius, shadow, display, position, and z-index families — plus a `token-bridge.css` alias layer that wires the utilities' color classes up to your acss-kit theme roles.

No build step, no JIT purge — the bundle is complete and you drop it in.

## Add it to your project

<Steps>

1. **Run `/utility-add`** to copy the bundle into your project:

   ```text
   /utility-add
   /utility-add --target=src/styles
   /utility-add --families=spacing,flex,color-bg
   /utility-add --no-bridge
   ```

   It detects your utilities directory (default `src/styles/`), copies `utilities.css`, and — unless you pass `--no-bridge` — copies `token-bridge.css` too.

2. **Import in your entry file.** Import the bridge **before** the utilities so the color aliases are defined when the utility classes resolve them:

   ```ts
   import "./styles/token-bridge.css"; // first
   import "./styles/utilities.css"; // then
   ```

</Steps>

If you have already run `/setup` for components and themes, `/utility-add` reuses the same project config (`.acss-target.json`). Utilities work standalone too — without an acss-kit theme, the bridge's hex fallbacks resolve to sensible fpkit-default colors.

## Commands

| Command                                                                  | Purpose                                                                    |
| ------------------------------------------------------------------------ | -------------------------------------------------------------------------- |
| [`/utility-add`](/acss-plugins-docs/acss-kit/commands/utility-add)       | Copy `utilities.css` (and `token-bridge.css`) into your project.           |
| [`/utility-list`](/acss-plugins-docs/acss-kit/commands/utility-list)     | Read-only catalogue printer — list families, or every class in one family. |
| [`/utility-tune`](/acss-plugins-docs/acss-kit/commands/utility-tune)     | Adjust the spacing baseline, breakpoints, or which families are enabled.   |
| [`/utility-bridge`](/acss-plugins-docs/acss-kit/commands/utility-bridge) | Regenerate `token-bridge.css` aliases against your active theme.           |

## Using the classes

Compose classes directly in your markup:

```tsx
// Flex row: spaced children, centered vertically, padded
<div className="flex items-center justify-between p-4 gap-3">…</div>

// Surface card with muted border and rounded corners
<article className="bg-surface border-border rounded-lg shadow p-6">…</article>

// Primary-colored heading text
<h2 className="text-primary font-semibold text-2xl mb-2">Plan</h2>
```

Spacing uses a `0.25rem` baseline (`.m-4` = `1rem`, `.p-8` = `2rem`) and emits CSS logical properties, so `mt`/`mb`/`ml`/`mr` follow the document's writing mode.

## Responsive variants

Responsive variants use a plain hyphen prefix — no escaping needed. Prefixes are `sm` (30rem), `md` (48rem), `lg` (62rem), `xl` (80rem), plus `print`:

```tsx
<div className="hide sm-show">Mobile-only header</div>
<button className="bg-primary p-4 md-p-6 lg-p-8">Padded button</button>
```

Run `/utility-list` to see the enabled families and the spacing/breakpoint scales, or `/utility-list spacing` for every class in a family.

## Utility families

| Family         | Classes include                                            | Responsive |
| -------------- | ---------------------------------------------------------- | ---------- |
| `color-bg`     | `.bg-primary`, `.bg-surface`, `.bg-danger`, …              | No         |
| `color-text`   | `.text-primary`, `.text-muted`, `.text-inverse`, …         | No         |
| `color-border` | `.border-border`, `.border-primary`, …                     | No         |
| `spacing`      | `.m-4`, `.p-8`, `.gap-3`, `.px-6`, …                       | Yes        |
| `display`      | `.flex`, `.grid`, `.block`, `.hide`, `.show`, …            | Yes        |
| `flex`         | `.items-center`, `.justify-between`, `.flex-col`, …        | Yes        |
| `grid`         | `.grid-cols-2`, `.col-span-3`, …                           | Yes        |
| `type`         | `.font-semibold`, `.text-sm`, `.text-2xl`, `.uppercase`, … | No         |
| `radius`       | `.rounded`, `.rounded-lg`, `.rounded-full`, …              | No         |
| `shadow`       | `.shadow`, `.shadow-md`, `.shadow-lg`, …                   | No         |
| `position`     | `.relative`, `.absolute`, `.fixed`, `.sticky`, …           | No         |
| `z-index`      | `.z-10`, `.z-50`, …                                        | No         |

<ColorSwatch
  roles={["primary", "surface", "danger", "success"]}
  table={false}
/>

## The token bridge

`utilities.css` references fpkit-style token names (`--color-error`, `--color-secondary`, `--color-primary-light`) that don't all exist in acss-kit's role catalogue. `token-bridge.css` is the alias layer that maps them onto acss-kit's roles:

| fpkit utility token     | acss-kit role alias                      |
| ----------------------- | ---------------------------------------- |
| `--color-error`         | `--color-danger`                         |
| `--color-error-bg`      | synthesized via `color-mix(in oklch, …)` |
| `--color-secondary`     | `--color-primary-hover`                  |
| `--color-primary-light` | synthesized via `color-mix(in oklch, …)` |

Every alias is defined for both `:root` and `[data-theme="dark"]`. Regenerate it with `/utility-bridge` whenever your theme changes (a new `/theme-create`, a `/theme-update` edit, or a custom palette) so the utility color classes track your current colors.

<Aside type="tip">
  Run `/utility-bridge` after any theme change. If the bridge goes stale, color
  utility classes like `.bg-primary` and `.text-danger` fall back to their
  hardcoded hex values instead of tracking your theme roles.
</Aside>

## Related

- [Styles guide](/acss-plugins-docs/acss-kit/styles-guide) — the theming system the bridge aliases into
- [Component Guides index](/acss-plugins-docs/acss-kit/components) — components you can style alongside these utilities
- [utilities skill](/acss-plugins-docs/acss-kit/skills/utilities) — maintainer reference (token source, generation pipeline, quality gates)
- [Utility Families](/acss-plugins-docs/acss-utilities/utility-families) — full family reference
- [Token Bridge](/acss-plugins-docs/acss-utilities/token-bridge) — full alias mapping
